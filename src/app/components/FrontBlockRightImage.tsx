import { ReactNode } from 'react'
export const FrontBlockRightImage = ({ children }: { children: ReactNode }) => {
    return (
        <div className="front-block-right-image">
            {children}
        </div>
    );
}